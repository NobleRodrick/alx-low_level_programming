#include "main.h"

/**
 * _abs - gives us the abs of any number
 * @n: the number
 *
 * Return: the abs
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
