#include "main.h"
#include<string.h>
/**
 * _strcmp -  this compares two strings
 *    based on defined criteria
 * @s1: first str.
 * @s2: second str.
 * Return: 0.
 */

int _strcmp(char *s1, char *s2)
{
	int final;

	final = strcmp(s1, s2);
	return (final);
}
